<form method="post" action="{{ path('app_materiel_soiree_delete', {'id': materiel_soiree.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ materiel_soiree.id) }}">
    <button class="btn">Delete</button>
</form>
